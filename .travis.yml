before_script:
- node --version
- npm --version

cache:
  directories:
  - $HOME/.npm
# - $HOME/.cache/pip
# - $HOME/.cache/pipenv

dist: noble

git:
  depth: 1

install:
- npm install

language: generic

node_js: node

os: linux

script:
- npx eslint KiraExampleSafeRegEx.js
- npx eslint KiraExampleReDoS.js
