cache:
  directories:
  # - $HOME/.npm
  - $HOME/.cache/pip
  # - $HOME/.cache/pipenv

dist: noble

git:
  depth: 1

install:
- pip install git+https://github.com/PyCQA/docformatter.git

language: python

os: linux

python: 3.13

script:
- docformatter kira_example.py
