before_script:
- lsb_release --all
- python --version
- tryceratops --version

cache:
  directories:
  # - $HOME/.npm
  - $HOME/.cache/pip
  # - $HOME/.cache/pipenv

dist: noble

git:
  depth: 1

install:
- python -m pip install --upgrade pip
- pip install tryceratops

language: python

os: linux

python: 3.13

script:
- tryceratops kira_example_file.py --verbose
- tryceratops kira_example_file.py --verbose --experimental
