before_script:
- node --version

cache:
  directories:
  - $HOME/.npm
# - $HOME/.cache/pip
# - $HOME/.cache/pipenv

dist: jammy

git:
  depth: 1

install:
- npm install --save-dev @terascope/fetch-github-release

language: node_js

node_js: node

os: linux

script:
- npx fetch-github-release --version
