git:
  depth: 1

language: python

script:
- echo "Kira Goddess!"

deploy:
  provider: pages
  skip-cleanup: true
  local-dir: SashaDest
  github-token: $GITHUB_API_KEY
  keep-history: true
  on:
    branch: master
  project-name: Kristinita\\\'s Search
  verbose: true
  commit_message: bash "KiraDeploy.sh"
