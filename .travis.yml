before_script:
- node --version
- v8r --version

cache:
  directories:
  - $HOME/.npm
# - $HOME/.cache/pip
# - $HOME/.cache/pipenv

dist: jammy

git:
  depth: 1

install:
- npm install --global v8r

language: node_js

node_js: node

os: linux

script:
- v8r .htmlhintrc --verbose
- v8r .yamllint --verbose
