cache:
  directories:
  # - $HOME/.npm
  - $HOME/.cache/pip
  - $HOME/.cache/pipenv

dist: noble

git:
  depth: 1

install:
- pip install pipenv
- pipenv install --dev

language: python

os: linux

python: 3.13

script:
- python installation_scripts/gh_release_install.py
- pipenv run fd --version
- pipenv run tidy --version
