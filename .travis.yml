cache:
  directories:
  # - $HOME/.npm
  - $HOME/.cache/pip
  - $HOME/.cache/pipenv

dist: jammy

git:
  depth: 1

install:
- pip install pipenv
- pipenv install --dev

language: python

os: linux

python: 3.13

script:
- python installation/github-release-install.py
- pipenv run fd --version
- pipenv run tidy --version
