cache:
  directories:
  # - $HOME/.npm
  - $HOME/.cache/pip
  # - $HOME/.cache/pipenv

dist: jammy

git:
  depth: 1

install:
- pip install gh-release-install

language: python

os: linux

python: 3.13

script:
- gh-release-install Kristinita/tidy-html5 tidy.exe KiraNonexistentDirectory/KiraNonexistentSubdirectory --verbose
