cache:
  directories:
  - $HOME/.npm
# - $HOME/.cache/pip
# - $HOME/.cache/pipenv

dist: noble

git:
  depth: 1

install:
- npm install --global json

language: generic

node_js: node

os: linux

script:
- npx eslint KiraExampleReDoS.js
