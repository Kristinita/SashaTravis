cache:
  directories:
  - $HOME/.npm
# - $HOME/.cache/pip
# - $HOME/.cache/pipenv

dist: noble

git:
  depth: 1

install:
- npm install --global json

language: node_js

node_js: node

os: linux

script:
- json -e "this['1.2.0']='messages/1.2.0.txt'" -f messages.json --in-place --output json-tab
- json -e "this['1.11.0']='messages/1.11.0.txt'" -f messages.json --in-place --output json-tab
- cat messages.json
